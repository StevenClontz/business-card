\documentclass[10pt]{scrbook}
\usepackage{ticket}
\usepackage{graphics}
\usepackage{graphicx}
\usepackage[noheadfoot,nomarginpar,margin=0pt,paperwidth=90mm,paperheight=52mm]{geometry}
\usepackage{color}
\usepackage{amsthm}
\usepackage{tikz}

\usepackage{paratype}
\renewcommand*\familydefault{\sfdefault} %% Only if the base font of the document is to be sans serif
\usepackage[T1]{fontenc}

\ticketNumbers{1}{1}

\unitlength=1mm

\ticketSize{90}{52} %in unitlength
\ticketDistance{0}{0} %in unitlength

%adjust these margins using the 'boxed' option with ticket, e.g. \usepackage[boxed]{ticket}
\advance \voffset -3.5mm
\advance \hoffset -0.1mm

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Here we set top and left margins found from the Avery Word-template (page setup margins)  %
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%
% define something
\definecolor{mainColor}{RGB}{0,32,91}
\definecolor{secondaryColor}{RGB}{191,13,62}

% make your default ticket. \ticketdefault is somewhat like a background
% here it is empty, because we use different tickets for the front an
% the back of our card
\renewcommand{\ticketdefault}{%
}

\newcommand{\puzzle}{
\input{puzzle.tex}
}

\newcommand{\theorem}{\scriptsize%
    \textbf{%
      Theorem: An integer is a multiple of nine if and only if
      the sum of its digits is a multiple of nine.
    }\newline
    \textit{Proof:}
    Express the integer in terms of its digits:
    \(d_0+10d_1+100d_2+\dots+10^nd_n\).
    This may be grouped into the sum of its digits
    \(d_0+d_1+\dots+d_n\) plus the sum
    \((10-1)d_1+(100-1)d_2+\dots+(10^n-1)d_n\). Since each
    \(10^i-1\) is divisible by nine...

    \hfill{\color{gray} clontz.org/card}
}


\pagestyle{empty}
\parindent=0pt

\begin{document}
\sffamily

\ticket{
    \put(6,46){\color{mainColor}\rule{58mm}{0.2mm}}
    \put(25,6){\color{mainColor}\rule{58mm}{0.2mm}}
    % \put(52,8){\includegraphics[height=36mm]{qrcode.pdf}}
    \put(8,38){\LARGE\color{mainColor} Steven Clontz}
    \put(8,33){\color{secondaryColor}mathematician, professor, puzzler}
    \put(8,9){\makebox[72mm]{\hfill\color{mainColor} steven.clontz\color{secondaryColor}@gmail.com} }
    \put(8,13){\makebox[72mm]{\hfill\color{mainColor}{\color{secondaryColor}http://}clontz.org} }
 }

\ticket{
    \put(45,12){\color{gray}\puzzle}
    \put(6.5,30){\parbox{35mm}{\theorem}}
    \put(20,5){\color{gray}\Large\texttt{\_\_\_ \_\_\_\_\_\_ \_\_\_\_\_\_\_}}
}

\end{document}
